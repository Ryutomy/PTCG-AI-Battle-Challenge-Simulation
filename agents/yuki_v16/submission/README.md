# Yuki v16 Submission Artifact

このディレクトリには、Kaggleから回収した次の最終提出アーカイブをローカル配置します。

```text
marnie_grimmsnarl_deck_yuki_v16_submission.tar.gz
```

SHA-256:

```text
87CD0DF2560E0775257958553700E7E446015D82A354C2B559C461C0D582CB96
```

アーカイブには自作方針コードだけでなく、コンペティション提供の`cg`ランタイムと共有ライブラリが含まれます。再配布条件とコードの帰属を確認するまで、`.gitignore`によりアーカイブと展開物をGit追跡対象外にしています。

公開用にコードを抽出する場合は、次を先に行います。

1. Kaggle Competition Rulesおよび同梱ランタイムのライセンスを確認する
2. 参考Notebook由来のコードと独自実装を区別する
3. ユーザー固有パス、トークン、キャッシュがないことを確認する
4. 公開対象のファイルだけを別ディレクトリへ抽出する
5. 60枚、entry point、最低限の起動確認を再実施する
